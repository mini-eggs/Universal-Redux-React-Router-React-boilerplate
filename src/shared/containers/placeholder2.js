import * as React from 'react';
import { connect } from 'react-redux';
import PlaceholderTwo from '../components/placeholder2';

const getProps = state => {
  return {};
};

export default connect(getProps)(PlaceholderTwo);
